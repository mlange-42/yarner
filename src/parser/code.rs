//! The parser for code files.

use crate::config::LanguageSettings;

/// Parses code files
pub struct CodeParser {}

impl CodeParser {
    /// Parse a code file in a specified language
    pub fn parse(&self, _source: &str, _language: &LanguageSettings) {}
}
